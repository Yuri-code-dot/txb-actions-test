name: TXB Test

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: |
          echo "Hello Yuri!"
          echo "TXB Actions is alive!"
          date
